import 'package:flutter/material.dart';

class HistoryScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('История запросов')),
      body: Center(child: Text('Здесь будет история запросов')),
    );
  }
}
